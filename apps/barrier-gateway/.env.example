# barrier-gateway -- worker de poll de apertura de pluma
# Copiar a .env y rellenar. El .env real NO se comitea (.gitignore).

# API de Vistara, sin barra final. El worker pega a  {VISTARA_API_BASE}/barrier/poll
VISTARA_API_BASE=https://vistara-api.condominioreserva.com/api/v1
VISTARA_TENANT_SLUG=la-reserva

# Device key de CUALQUIER device activo del tenant (sirve solo para autenticar el
# poll — no necesita ninguna config especial). Puede ser la del OCR de lpr-caseta,
# o una dedicada creada con libs/prisma/scripts/create-device.ts.
VISTARA_DEVICE_KEY=

# Cada cuántos segundos preguntar por comandos.
POLL_SECONDS=2

# Endpoint GPIO del tótem CondoVive en la LAN (el mismo que usa
# lpr-caseta/src/totem_gpio.py). El segmento final es la clave adb_device
# de la tabla dev_device del tótem, no una IP:puerto real.
TOTEM_GPIO_URL=http://192.168.196.1:3001/devices/gpio/192.168.100.5:5555
# Opcional -- solo si el tótem algún día agrega auth. Vacío = no se manda header.
TOTEM_GPIO_TOKEN=
TOTEM_TIMEOUT_MS=5000

# Anti-rebote: ignora una segunda apertura dentro de esta ventana (ms).
BARRIER_MIN_INTERVAL_MS=4000
