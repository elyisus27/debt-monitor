{
  "name": "debt-monitor",
  "private": true,
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "dev:api": "pnpm --filter @debt-monitor/api dev",
    "dev:web": "pnpm --filter @debt-monitor/web dev"
  },
  "devDependencies": {
    "turbo": "^2.3.0"
  }
}
